const actions = {
  /**
   * Water action adds water to the crops.
   */
  water: { type: 'water' },
  /**
   * Light action adds light to the crops.
   */
  light: { type: 'light' },
};

export default actions;
